# Allow opening any sourced file, even if not specified as input
external-sources=true

# disable warnings about no expansions in single quotes
disable=SC2016

# this is a lib of Bash functions, after all
shell=bash
